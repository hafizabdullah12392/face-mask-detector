name: face-mask-detector
description: Detects whether a person is wearing a face mask using a trained PyTorch model.
input:
  image:
    type: image
    description: Input image
output:
  type: string
  description: Prediction result
predict: "predict.py:predict"
